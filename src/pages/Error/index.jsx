import React from 'react';
import Helmet from 'react-helmet';

import { NavWrapper } from '../../components/NavWrapper';

function About() {

    return (
        <NavWrapper>
            <Helmet><title>Totem Learning</title></Helmet>
            Sorry, cannot find that page.
        </NavWrapper>
    );
}

export default About;
